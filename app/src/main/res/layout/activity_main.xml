<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/hoteel"
        android:scaleType="center"
        android:screenReaderFocusable="true"
        />

    <TextView
        android:padding="20dp"
        android:textSize="14dp"
        android:textColor="#fafafa"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Please Press Button below to scan for resturant"
        android:id="@+id/idholder"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Scan"
        app:layout_constraintEnd_toEndOf="@+id/idholder"
        app:layout_constraintStart_toStartOf="@+id/idholder"
        app:layout_constraintTop_toBottomOf="@+id/idholder" />


</androidx.constraintlayout.widget.ConstraintLayout>